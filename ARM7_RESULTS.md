# ARM7_RESULTS —— arm7 离线评测的全部结果与分析

> 日期：2026-09-05。对象：`outputs/arm7__seed42_fi3_512_aug_sr`。
> 训练仍在进行（写作时 step ~6000/10000），本文覆盖 **step 4000** 的完整读数、
> **step 6000** 的部分读数，以及全部对照实验。
>
> 配套：`EVAL_PLAN_ARM7.md`（评测方案）、`CROSSMODAL_FUSION_PLAN.md`（联合解码与跨模态监督）、
> `MODE_MIX_MASKS.md` §9.5（mask 轴）、`paper/EXPERIMENT_STATUS_zh_aug29.md` §四十五（流水账）。

---

## 0. 一页摘要

| # | 结论 | 状态 |
|---|---|---|
| 1 | ~~四条输入路径等价~~ **作废**。n=80 复测显示：**从 step 4000 起，非 RGB 路径就一直比 RGB 差 ~1.2–1.3×**（4000: depth p=0.033 / seg p=0.018；8000: normal p=0.005）。此前的「等价」是 **n=16 低功效假象** | **已推翻，见 §4.3** |
| 1' | **训练确实教会了模型用非 RGB 观测行动**，只是没教到平级：depth 从没训过的 2.10× 收窄到 1.25×（**缩小 77%**），seg 1.51×→1.18×（64%），normal 1.57×→1.36×（38%） | **成立** |
| — | **负对照有效**：同一把尺子量只训过 RGB 的模型，depth 那一问退化 2.10×，16 个 episode 输 15 个，p=0.001 | **成立** |
| 2 | **按 RGB 曝光对齐后，arm7 的 RGB 不比专家差**：两个对齐点 1.08×(p=0.454) 与 0.97×(p=0.804)，均分不出差别 | **成立（此前的相反结论已作废）** |
| 3 | arm7 生成 depth / normal / scene-role 图都明显强于专家（专家没训过这些） | **成立** |

**最重要的一件事**：结论 2 的第一版是「arm7 的 RGB 差 1.31×」，那是**对照口径错了**——
详见 §5。修正后结论反转。

---

## 1. 实验设置

| | |
|---|---|
| 被测 | `arm7` checkpoint-4000（`video+action@0.4, depth+action@0.2, segmentation+action@0.2, normal+action@0.2`，`--action_mask_mix A1`，`scene_roles`） |
| 对照 | `specialist_action`（`video+action@1.0`，同 warm start / seed / 树 / fi=3 / lr）checkpoint-1500 / 2500 / 4000 |
| 数据 | `rlbench_selfgen_512_aug`，8 个任务 × {variation0 = seen, variation1 = unseen} = **16 个 episode** |
| 推理 | `conditioning_mode=iiii`，cfg 7.5，50 步，seed 42，res 512，`frame_interval` 3，`prompt_tag_style` explicit |
| 脚本 | `scripts/heldout_batch_eval.py`（本轮扩展，见 §7） |
| 产出 | `reports/heldout_batch_eval/{arm7_4000, arm7_6000, negctl_actionspec_4000, negctl_actionspec_4000_unseen, spec_1500_matched, spec_2500_matched}` |

`specialist_action` 是**完全独立的另一个模型**，不是 arm7 内部的模块，也不是 router 里的
expert。它在本文里担两个角色：**竞争对手**（目前最强的闭环模型，46.0%）与**负对照**（§3）。

---

## 2. 尺子：`pos_err_m` 是怎么量出来的

四条路径共用**同一条测量链**，这是「等价」这个说法能成立的前提。

```
1. 给模型一个 prompt tag + 一个锚定帧          <depth><action> + depth 编码图
2. 扩散采样 50 步，生成 4 段序列               D0 | A0 | D1 | A1
3. 取【第 1 段和第 3 段】= 两个视角的 action 热图
   （段序 view-major、action 在视角内最后，所以四个模板的 action 段都在 index 1 和 3）
4. fuse_multiview_heatmaps_to_pose_torch(热图, 外参, 内参)
   → 每帧一个 6-DoF 位姿 pose8
5. pos_err[t] = ‖pose8[t, :3] − GT_action_7d[t, :3]‖          （米）
6. 这一格的值 = median_t(pos_err)                              41 帧的中位数
```

关键点：

- **第 4–6 步对四条路径完全相同**。解码器、外参内参、GT 轨迹都是同一份。
  唯一变的是第 1 步喂进去的锚定帧像素和 prompt tag。
- **GT 是同一条**。四条路径描述的是同一个 episode 同一个时间窗的同一段机械臂运动，
  所以它们的误差是可以直接比的，不存在「各看各的、标准不同」的问题。
- 一格 = 一个 episode 的一个数（41 帧误差的中位数），单位是**米**。
  16 个 episode × 4 条路径 = 64 格。

---

## 3. 「等价」的精确含义

这是本文最需要讲清楚的一节。

### 3.1 为什么看**比值**而不是绝对值

绝对误差里混了两样东西：**模型整体有多好** 和 **这条路径相对其他路径有多好**。
我们要问的是后者。所以每条路径都除以**同一个模型自己的 RGB 路径**：

```
比值 = mean(该路径的 pos_err) / mean(同一模型 RGB 路径的 pos_err)
```

这样模型整体水平被约掉了。比值 1.00× 表示「换成这个观测，答案一样好」；
2.10× 表示「换成这个观测，误差翻了一倍」。

### 3.2 为什么必须**配对**

四条路径跑的是**同一批 16 个 episode**，而 episode 之间难度差异极大
（§4 的表里 0.05 到 0.56 都有）。不配对地比两组均值，难度差异会淹没路径差异。
配对之后，每个 episode 内部自己和自己比，难度被消掉。

### 3.3 p 值是什么

**双尾符号检验**：数「该路径比同模型 RGB 更好的 episode 有几个」，
在「两者无差别」的零假设下这个计数服从 Binomial(16, 0.5)，算双尾概率。

它只用胜负、不用差值大小，所以对重尾不敏感——而我们的数据**确实是重尾的**（§7.1）。

### 3.4 表格，以及每个 p 值的来源

| 问法 | arm7 比值 | arm7 胜场 | p | | spec 比值 | spec 胜场 | p |
|---|---:|---:|---:|---|---:|---:|---:|
| depth | **0.98×** | 8/16 | 1.000 | | **2.10×** | **1/16** | **0.001** |
| seg | 1.03× | 6/16 | 0.454 | | 1.51× | 4/16 | 0.077 |
| normal | 0.95× | 10/16 | 0.454 | | 1.57× | 4/16 | 0.077 |

逐个读：

- **arm7 depth 8/16, p=1.000**：胜负正好各半，这是零假设下最典型的结果。
- **arm7 seg 6/16 / normal 10/16, p=0.454**：偏离一半 2 场，在 n=16 下完全在噪声内。
- **spec depth 1/16, p=0.001**：16 个 episode 输了 15 个。这不可能是巧合。
- **spec seg / normal 4/16, p=0.077**：输 12 个，方向明确但没到 0.05。

### 3.5 为什么右边那半张表是**必需**的

只有左边的话，「arm7 四条路差不多」有两种解释：

1. arm7 真的学会了四种观测空间；
2. **这把尺子太钝**，什么都量不出差别。

右边排除了第二种：**同一把尺子、同样的 16 个 episode、同样的解码链**，
用在一个只训过 `video+action` 的模型上，depth 那一问立刻塌掉 2.10×、p=0.001。

**尺子能分辨「训过 / 没训过」。而 arm7 在同样的分辨力下测不出退化。**
两半合起来才是证据，单看任何一半都不是。

### 3.6 这个结论**不**包含什么

- 不是说四条路径的**表示**相同——只是说解出来的动作**质量**分不出差别。
- 不是说 arm7 比专家强——那是结论 2，另一回事。
- 不是说 seg/normal 路径在闭环里能用——仿真器只渲染 RGB（`eval/rollout_env.py:187`），
  这三条路目前只能离线读。
- n=16，只能排除较大的效应。若真实差异是 5% 量级，这个实验看不出来。

---

## 4. 结论 1 的原始数据（arm7@4000，逐 episode，单位米）

| episode | split | RGB | depth | seg | normal |
|---|---|---:|---:|---:|---:|
| `close_jar` | seen | 0.1315 | 0.1091 | 0.2282 | 0.0523 |
| `close_jar` | unseen | 0.1247 | 0.1502 | 0.1392 | 0.1273 |
| `insert_onto_square_peg` | seen | 0.1004 | 0.0934 | 0.0712 | 0.0859 |
| `insert_onto_square_peg` | unseen | 0.1827 | 0.2471 | 0.1790 | 0.1654 |
| … | | | | | |
| **均值（16 个）** | | **0.1865** | **0.1835** | **0.1919** | **0.1773** |

完整逐 episode 表见 `reports/heldout_batch_eval/arm7_4000/per_episode.json`。

分 split 的均值：

| split | RGB | depth | seg | normal |
|---|---:|---:|---:|---:|
| seen (n=8) | 0.1434 | 0.1339 | 0.1628 | 0.1385 |
| unseen (n=8) | 0.2296 | 0.2331 | 0.2210 | 0.2161 |

unseen 整体差约 60%，这是**任务泛化**的代价，四条路径同步变差，
没有哪一条在 unseen 上特别抗打。

### 4.3 结论 1 被 n=80 推翻：从来就不等价

**2026-09-06 更正。** §4.3 此前的版本说「等价在 4000 成立、6000/7000 走弱」。
用 n=80 在**同一个 checkpoint-4000** 上复测，这个说法是错的。

| 路径 | @4000 n=**16** | @4000 n=**80** | @8000 n=**80** |
|---|---|---|---|
| depth | 0.98× 8/16 p=1.000 | **1.31× 30/80 p=0.033** | 1.25× 32/80 p=0.093 |
| seg | 1.03× 6/16 p=0.454 | **1.28× 29/80 p=0.018** | 1.18× 32/80 p=0.093 |
| normal | 0.95× 10/16 p=0.454 | 1.18× 40/80 p=1.000 | **1.36× 27/80 p=0.005** |
| 汇总 | 24/48 p=1.000 | 99/240 p=0.008 | 91/240 p=0.0002 |

**同一个 checkpoint，n=16 说「完美等价」（24/48, p=1.000），n=80 说「depth/seg 显著更差」。**
差别只在样本量。所以：

- 「四路等价」**从来不成立**，是低功效假象；
- 「随训练退化」**也不成立**——4000 和 8000 的汇总计数（99/240 vs 91/240）差不多，
  没有系统性趋势。之前看到的「退化」是 n=16 的抖动。

**正确的表述**：从 step 4000 起，非 RGB 路径就稳定地比 RGB 差约 1.2–1.3×。

### 4.3.1 但训练确实起作用了 —— 用负对照做参照系

只看「比 RGB 差 1.25×」会低估训练的效果。正确的参照是**没训过这条路的模型**：

| 路径 | 没训过（spec@4000） | arm7@4000 | arm7@8000 | 差距缩小 |
|---|---:|---:|---:|---:|
| depth | 2.10× | 1.31× | 1.25× | **77%** |
| seg | 1.51× | 1.28× | 1.18× | **64%** |
| normal | 1.57× | 1.18× | 1.36× | 38% |

（差距缩小 = 从「没训过」走向理想值 1.00× 走了多远。）

**arm7 把「从 depth 观测行动」这件事学掉了 77% 的差距，但没学到平级。**
这是一个比「等价」更弱、但**有证据支持**的主张。

### 4.4 教训：n=16 在这个测量上是不够的

同一个 checkpoint、同一把尺子，n=16 与 n=80 给出**相反**的结论，而且这件事发生了**两次**
（4000 的「等价」、6000/7000 的「退化」）。

相邻 checkpoint 之间同一路径的均值抖动实测 5–20%，与要测的效应（20–30%）同量级。
**结论：这类比较一律用 n=80，n=16 的历史读数只能当噪声。**

---

## 5. 结论 2：对照口径的更正（第一版结论已作废）

### 5.1 错在哪

第一版比的是 **arm7@4000 vs spec@4000**，理由是「都是 4000 步，且两者 action 样本率都约 90%，
所以 action 更新次数匹配」。

那匹配的是**总 action 曝光**。但要回答「另外三个模态有没有帮到 **RGB 这条路**」，
必须按 **`video+action` 曝光**匹配：

```
arm7 每步以 0.4 的概率抽到 video+action；spec 是 1.0。
10% 的 action dropout 两边相同，约掉。
    匹配条件：S × 0.4 = N
      arm7@4000  ↔  spec@1600   （≈ ckpt-1500）
      arm7@6000  ↔  spec@2400   （≈ ckpt-2500）
      arm7@10000 ↔  spec@4000   ← 只有到终点，spec@4000 才是正确对照
```

**arm7@4000 的 RGB 曝光只有 spec@4000 的 40%。** 拿它去比 spec@4000，
差距里至少有一部分（可能全部）只是训练量少了 2.5 倍。

### 5.2 修正后的结果

`specialist_action` 的 HF 仓库（`TingtingDu/specialist_action__seed42_fi3_512_aug_sr`）
存了 500–4000 每 500 步的 checkpoint（本地只剩 4000，其余早被剪掉），拉回 1500 与 2500 重测：

| RGB 曝光 | 对比 | arm7 均值 | spec 均值 | 比值 | arm7 胜场 | p |
|---:|---|---:|---:|---:|---:|---:|
| ~1600 | arm7@4000 vs **spec@1500** | 0.1865 | 0.1726 | **1.08×** | **10/16** | 0.454 |
| ~2400 | arm7@6000 vs **spec@2500** | 0.1461 | 0.1512 | **0.97×** | 9/16 | 0.804 |
| ~4000 | arm7@10000 vs spec@4000 | 待训练结束 | 0.1429 | — | — | — |
| — | ~~不对齐~~ arm7@4000 vs ~~spec@4000~~ | 0.1865 | 0.1429 | ~~1.31×~~ | ~~4/16~~ | ~~0.077~~ |

**对齐之后，「arm7 的 RGB 更差」不成立。** 两个对齐点分别是 1.08×（arm7 胜 10/16，p=0.454）
与 0.97×（胜 9/16，p=0.804）—— 都分不出差别，且第二点已经越到 arm7 这一侧。
趋势（1.08 → 0.97）是有利的，但 n=16 分辨不出这个量级，**不能当成结论**。

注意均值（1.08×，arm7 略差）与胜场（10/16，arm7 略好）**方向相反**，这是重尾：
arm7 赢的次数更多，但输的时候输得更大。这条必须如实写明，不能只挑一个说。

### 5.3 还没做完的

`arm7@10000 vs spec@4000`（第三个点，也是**唯一一个 spec 已跑完的对照**）等训练结束（~22h）。

---

## 6. 结论 3：生成质量（白捡的副产物）

扩展脚本时打开了顶替模板里 **anchor 段**的评分（§7.3），于是免费得到：

| 指标 | arm7@4000 | spec@4000 | 方向 |
|---|---:|---:|---|
| depth AbsRel（`depth+action` 内生成） | **0.1315** | 0.2267 | 低好 |
| normal mean_cos（`normal+action` 内生成） | **0.8805** | 0.3699 | 高好 |
| seg macro_mIoU（`segmentation+action` 内生成） | 0.4032 | 0.4073 | 高好 |

**seg 那一行是误导性的**，逐角色查过（`per_role_iou` 本来就存在 JSON 里）：

| role | n | arm7 | spec | 差 |
|---|---:|---:|---:|---:|
| background | 16 | 0.8239 | 0.8260 | −0.0021 |
| robot_arm | 16 | 0.3283 | 0.1863 | **+0.1420** |
| distractor | 4 | 0.4390 | 0.2963 | **+0.1427** |
| fixture | 8 | 0.8516 | 0.7122 | **+0.1393** |
| goal | 8 | 0.4366 | 0.3468 | +0.0898 |
| target | 15 | 0.3913 | 0.3392 | +0.0521 |
| gripper | 16 | 0.0859 | 0.0599 | +0.0260 |
| tool | 2 | 0.3917 | 0.4687 | −0.0770 |

**剔除 background 后：arm7 0.4177 vs spec 0.3442**，arm7 在 8 个角色里赢 6 个。
macro-mIoU 被 background（两边都 ~0.82、最容易）稀释了 ——
`modality_mode_grid.py::score()` 的注释本来就警告过 macro-mIoU 跨任务不可比，我第一遍没照做。

顺带：**arm7 在 `depth+action` 里生成的 depth 是 AbsRel 0.1315**。这是此前无法测量的量；
之前我错误引用 arm6 的 0.1441（不同模板 + 不同 conditioning，不可比）。

---

## 7. 方法论错误清单

本轮一共踩了四个坑，全部影响过已发布的数字，逐条记下。

### 7.1 中位数在 n=8 重尾下是错的统计量

负对照里 `action_from_seg` 的中位数 0.0994 比 RGB 的 0.1166 **还好**，看起来像仪器坏了。
逐 episode 是 0.4273 / 0.0491 / 0.4238 / 0.0397 …，**均值 0.1768** 才是实情。

**此后一律用「配对 + 均值 + 符号检验」，不用裸中位数。**

### 7.2 跨 run 比较必须先核对 episode 集合

存档基线 `actionspec_4000_matched` 报 0.1405，跑的是 **80 个** episode（每任务 5 个 × 2 variation）；
`heldout_batch_eval.py` 的默认只有 **8 个**。同一 checkpoint、同一指标：

```
0.1405 (n=80)   vs   0.1166 (n=8)      20% 的差异纯粹来自 episode 集合
```

幸好负对照里把 `action` 一行也跑了（本来只当回归自检），它才是真正同集合的基线。

### 7.3 匹配曝光的口径（§5，最严重的一个）

「总 action 曝光」≠「`video+action` 曝光」。用错口径把结论比反了。

### 7.4 macro 指标会被简单类别稀释（§6）

---

## 8. 本轮修掉的两个代码 bug

| bug | 后果 |
|---|---|
| `heldout_batch_eval.py` 评 anchor 段时**无条件找 `video` 段**（注释称「每个模板第一段都是 video」） | 对 arm7 的顶替模板是假的，`next()` 抛 StopIteration。改为从 plan 取 anchor 后，顺带产出 §6 |
| `heldout_batch_eval.py` 的 **`--gpu` 完全失效** | 它在 `main()` 里才设 `CUDA_VISIBLE_DEVICES`，而 import 该模块时 CUDA 已初始化（`torch.cuda.is_initialized()==True`），之后该变量不生效 —— **此前每一次运行都悄悄落在物理 GPU 0**。两个并发评测因此抢同一张卡，一个 OOM 而日志却打印 "using GPU 7"。现改为检测到该情形直接**拒绝启动**并给出正确用法：`CUDA_VISIBLE_DEVICES=<n> python ... --gpu 0` |

`MODALITY_TEMPLATE` 新增了 `action_from_depth` / `action_from_seg` / `action_from_normal`
三个 key，**原有四个 key 一字未动**，历史运行全部仍可复现。

---

## 9. 下一步

| 待办 | 依赖 |
|---|---|
| `arm7@6000 vs spec@2500`（第二个对齐点） | 在跑 |
| `arm7@10000 vs spec@4000`（第三个点，最终判据） | 训练结束（~22h） |
| 闭环要不要跑（11.3 GPU-h × 2） | **等第三个点**。原本打算用「离线闸门说 RGB 更差」来省这笔钱，但那个判断建立在 §7.3 的错口径上，已作废 |
| 联合解码 / 跨模态监督 | 见 `CROSSMODAL_FUSION_PLAN.md`；其 Phase A 的门控条件（「特权路径明显强于 RGB」）**目前不成立** —— 四条路等价意味着没有可蒸馏的差距，这一条要重新考虑 |

---

## 10. 复现

```bash
# arm7 Tier-1（四条路径 × 16 episode）
CUDA_VISIBLE_DEVICES=<n> PYTHONPATH=. python -u scripts/heldout_batch_eval.py \
  --ckpt outputs/arm7__seed42_fi3_512_aug_sr/checkpoint-4000/step4000.ckpt \
  --tag arm7_4000 \
  --modalities action,action_from_depth,action_from_seg,action_from_normal \
  --gpu 0 --frame_interval 3 --res 512 --segmentation_mode scene_roles --seed 42

# 负对照（同上，换 ckpt 为 specialist_action）
# 匹配曝光对照（--modalities action，ckpt 换 checkpoint-1500 / 2500）
```

`--frame_interval 3` / `--res 512` / `--segmentation_mode scene_roles` /
`--prompt_tag_style explicit` 任何一项不一致都会**静默**让读数作废。
